# 2026-09-10

## AI 面板打开 / 切会话“从头滚到尾”（已修）
- 根因：消息 effect 用 `scrollIntoView({ behavior: 'smooth' })`，打开面板（`leftActivity==='ai'`）、切换会话、载入历史时也走 smooth → 视觉上从消息顶部一路滚到底部。
- 修复：新增 `aiInstantScrollRef` 标记——①刚进入 AI 面板时置位；②切换会话的 `useLayoutEffect`（瞬时 `scrollTop`）也置位；滚动 effect 读到标记则直接 `scrollTop = scrollHeight`（瞬时）并清标记。**新消息追加 / 生成中仍保留 smooth**。
- 验证：`npx tsc --noEmit` 通过。

## AI 面板「清除全部对话」（新增）
- 原 Trash2 按钮只清空当前对话（清 messages 保留会话）；用户要“清除整个”。
- 现在两个按钮：`Eraser`=清空当前对话（原 `clearAiConversation`），`Trash2`=**清除全部对话**（新 `clearAllAiConversations`，`requestConfirm` 危险确认）。
- 实现：后端没有批量删除命令，前端逐个 `deleteAiConversation(id)`（会话量小，可接受）；全部删完后本地 `setAiWorkspace` 重置为一个 `createAiConversationState()` 空白会话——激活会话变化会触发已有的 600ms 防抖 `saveAiConversation`，空白会话自动落库。
- 按钮 disabled 条件：没有任何会话含消息时禁用。

## AI 设置窗口移除「模型列表」区块（用户要求）
- 删除 `AiSettingsWindow.tsx` 中 `<section class="ai-settings-section-models">`（可视化模型列表：filter 搜索、sync 按钮、当前模型选择、启用/禁用开关，95 行 JSX）。
- 一并清理死代码：`setAiDraftCurrentModel` / `toggleAiDraftEnabledModel` 两个函数、`aiModelListQuery` state、`openTab` 里的重置调用。
- **保留**：`resolveAiModelCatalog`（`submitAiProviderConfig` 等处仍用）、`syncAiModelsFromProvider`（区块外另有同步按钮）、`Search`/`X` 图标 import（其他区块使用）、models tab 其余内容（模型名文本输入等）。
- 语义变化：模型改由文本输入 + 区块外同步按钮管理，不再有可视化列表与启用开关。
- 验证：tsc + read_lints 通过。

## AI 设置窗口移除「API Keys 折叠标题」+ Models 面板「取消/保存 footer」（用户要求）
- 删除 `ApiSettingsWindow.tsx`（实为 `AiSettingsWindow.tsx`）models 面板中 `api-settings-collapse-title` 折叠按钮；正文 `<div class="ai-settings-collapse-body">` 改为常显（去掉 `{aiSettingsApiKeysOpen && (` 条件包裹）。
- 一并清理死代码：`aiSettingsApiKeysOpen` state、`addAiAccount` 里的 `setAiSettingsApiKeysOpen(true)`、不再使用的 `ChevronRight` import（`ChevronDown`/`ChevronUp` 在 MCP 区块仍在用）。
- 删除 models 面板底部 `<footer class="ai-settings-footer">`（取消 + 保存按钮，40 行）。MCP 面板的 footer（1692 行附近，取消/保存 MCP）**保留未动**。
- ⚠️ 后果：models 表单已无 submit 按钮，`submitAiProviderConfig` 只剩 `<form onSubmit>` 可触发，而 HTML 隐式提交在「多个 blocking 字段」时不生效 → **UI 上暂时无法保存模型配置**。若用户后续未补自动保存，需要提醒。
- 技巧：整块 JSX 去包裹后要左移缩进，用 PowerShell `[IO.File]::ReadAllLines` + 逐行 `Substring(2)` + `[IO.File]::WriteAllText($p, ($l -join "`r`n"))` 批量 dedent；**注意 `ReadAllLines` 会丢掉文件末尾换行**，需事后补回 `"`r`n"`，否则产生无关 diff。本仓库该文件为 CRLF、无 BOM。
- 验证：`npx tsc --noEmit` 通过，`read_lints` 无错误。

## AI 设置窗口「API 账号」标签样式统一（用户要求）
- `.ai-settings-account-chip`：`border-radius: 999px` → `8px`（与输入框/按钮/nav 项一致），并补 `transition`。
- 选中态去掉 GitHub 蓝（原 `rgba(88,166,255,0.45)/(0.12)`）→ 改为应用灰系 `border-color: rgba(var(--ai-fg-rgb),0.32)` + `background: rgba(var(--ai-fg-rgb),0.14)`，与 `.ai-settings-nav button.active` 同款。
- 约定已记入 `MEMORY.md` 第 6 节。

## 「测试模型」弹窗新增等待时间（用户要求）
- `AiSettingsWindow.tsx`：新增 state `aiProviderTestElapsedMs` + 一个 effect（`aiProviderTestDialog.status === 'running'` 时每 100ms 刷新 `Date.now() - startedAt`，结束后保留最终值）。
- 弹窗 `.dialog-actions` 里加 `<span class="ai-provider-test-elapsed">`：请求中显示「等待中 x」，结束后显示「等待时间 x」，用已有 `formatAiTestDurationMs`（<1s 显示 ms，否则 s）；CSS 用 `margin-right:auto` 顶到左侧、`tabular-nums` 防跳动。
- 作用：请求挂起时用户能看到实时耗时；失败分支后端未回传 `total_ms`，前端实测值仍可用。
- 验证：`npx tsc --noEmit` 通过。

## AI 设置新增「上下文窗口 / 最大输出 Token / 默认推理强度」（用户要求，跨前后端）
- 后端 `ai_config.rs`：`AiProviderAccountStore` 新增 `#[serde(default)] context_window: u32`（默认 `DEFAULT_AI_CONTEXT_WINDOW = 200_000`）与 `max_tokens: u32`（默认 `AI_DEFAULT_AI_MAX_TOKENS = 0`＝不限制）；新增 `validate_ai_context_window`（0→默认；1000~2_000_000）、`validate_ai_max_tokens`（≤1_000_000）、`ai_request_max_tokens`（0→None）、`AI_CLAUDE_FALLBACK_MAX_TOKENS = 8192`。`reasoning_effort` 仍是**全局**字段（在 `AiProviderConfigStore`，非账号级）。
- 后端 `main.rs`：DTO/`SaveAiProviderConfigRequest`/`ai_config_snapshot`/`save_ai_provider_config` 全部透出并写回新字段；`OpenAiChatRequest`/`OpenAiChatStreamRequest` 增加 `max_tokens: Option<u32>`（skip_if_none）；`build_claude_messages_payload` 增加 `max_tokens` 参数（0 时用 8192 兜底，替换原硬编码 8192）。`add_ai_provider_account` 里的账号字面量也要同步补字段（漏了会编译不过）。
- 前端：`api.ts` 类型/保存入参、`aiSettingsModel.ts` 的 `AiConfigDraft`/默认值/`isAiConfigDraftDirty`/`aiConfigToDraft`/`aiConfigToProviderState` 全部加字段 + `normalizeAiContextWindow`/`normalizeAiMaxTokens` + `AI_REASONING_EFFORT_OPTIONS`（避免把 aiChatModel 整个模块拉进设置窗口）。
- 设置页 UI：三行 label+控件（number input ×2 + SelectDropdown），保存时归一化；`submitAiProviderConfig` 改为提交草稿里的 `reasoning_effort`（原来只是把后端值原样回传）。
- 上下文窗口生效方式：`aiChatModel.ts` 新增 `resolveAiContextBudgetChars(tokens) = clamp(tokens×3.5, 4000, 200_000)`（200k 是后端 `MAX_AI_TOTAL_CHARS` 硬上限），`buildAiRequestMessages`/`estimateAiContextUsage` 接收预算，App.tsx 传入 `aiProviderConfig.context_window`。默认 200k token → 换算后被 200k 字符上限夹住 → **与改动前行为一致**，只有调小窗口才会真正收紧历史。
- `App.tsx:293 const isAiSettingsWindow = false;` → App.tsx 里那套 AI 设置表单（`isAiSettingsOpen` 面板，8241 行起）是**死代码**，设置窗口实际渲染 `AiSettingsWindow.tsx`（见 `main.tsx` windowMode==='ai-settings'），改 UI 只需动后者。
- 验证：`cargo check`、`cargo test`（52 passed，含新增 `serialize_ai_max_tokens_only_when_configured`）、`npx tsc --noEmit` 全部通过。

## 隐藏 number 输入框原生 spinner（用户反馈「上下加减按钮很丑」）
- 位置：`styles.css` 紧接 `.ai-settings-input:disabled` 之后（约 5739 行）。
- 内容：`.ai-settings-input[type='number']` 设 `appearance: textfield` + `-moz-appearance: textfield`；`::-webkit-outer-spin-button` / `::-webkit-inner-spin-button` 设 `appearance: none` + `margin: 0`。
- 生效范围：仅 `.ai-settings-input`（即设置页「上下文窗口」「最大输出 Token」两个 number 输入），键盘上下箭头调节仍保留。
- WebView2 是 Chromium，`-webkit-inner-spin-button` 生效；无需改 JSX。

## 上下文窗口 / 最大输出 Token 改为「留空 = 默认」（用户要求）
- 输入框不再预填数值：`DEFAULT_AI_CONFIG_DRAFT` 里两者都是 `0`，`normalizeAiContextWindow/normalizeAiMaxTokens` 把 `<=0` 一律保留为 `0`（= 未设置），保存时写 0。
- Rust：原 `DEFAULT_AI_CONTEXT_WINDOW/DEFAULT_AI_MAX_TOKENS` 两个常量合并为 `AI_UNSET_TOKEN_LIMIT = 0`（留空哨兵）；`validate_ai_context_window(0) → Ok(0)`；serde 默认值、`new_default_ai_account`、v1 迁移、`add_ai_provider_account` 全部写 0。
- 留空时的生效默认：上下文窗口 `AI_DEFAULT_CONTEXT_WINDOW = 400_000`（放在 `aiSettingsModel.ts`，供 placeholder 与 `resolveAiContextBudgetChars` 使用；依据 2026 主流旗舰 GPT-5.x 400K / Claude 4.5 200K~1M / Gemini ~1M）；最大输出 token 留空 = 不注入请求体。
- 为什么 max_tokens 不给固定数字：固定值会同时压低新模型（GPT-5.x 128K、Claude 4.5/Gemini 64K）的输出上限，并让 gpt-4o-mini（16384）/deepseek（8192）等端点直接报 `max_tokens` 超限错误；留空即由服务端按模型上限决定。
- 占位符：`默认 400000（token）` / `默认（不限制，交给服务端）`。旧配置若已存了具体数值仍会显示该值，清空即恢复留空。
- 验证：`cargo test` 52 passed、`npx tsc --noEmit`（TSC_EXIT=0）。

## 修复「低风险自动执行」开关无效（用户反馈：开了跟没开一样）
- 现象：Agent 模式下打开开关，动作卡片显示「自动执行」徽标，但动作仍是 `proposed`，还要手点「授权并执行」。
- 根因：`App.tsx` 的 `finishAiStream` 把解析结果写在 `updateAiConversation(...)` 的 **setState updater** 里（`autoRunCapture.job`、`shouldRepairAgentProtocol`），函数返回后立刻读取；React 18/19 只在 fiber 无 pending update 时才同步执行 updater，流式结束时最后一个 delta 的更新常未 flush → 读到 null → 自动执行与协议纠偏都失效（同一个坑，协议纠偏也一直没生效）。
- 修复：把「读取解析产物 → 协议纠偏 / 自动执行 / 冲刷消息队列」整块推迟到 `window.setTimeout(..., 0)`（React 调度走 MessageChannel，先于定时器回调）。
- 顺带修正徽标语义：原来所有低风险 `proposed` 动作都标「自动执行」，实际每条消息只会自动跑**第一个**低风险终端动作（无终端动作时才跑第一条 MCP，跑完由 `continueAgentAfterTerminal` 续跑让模型提出下一步）；现在徽标只用相同的选择逻辑标注真正会被执行的那一条。
- 纯前端改动，Vite 热更新即可，无需重启 tauri。

## 「自动执行」修复迭代（用户反馈：改成 setTimeout 后仍要手动点）
- 第一次修复只是把「读取解析产物」从同步改成 `window.setTimeout(0)`，**仍不可靠**（定时器与 React 调度（MessageChannel）谁先执行没有硬保证），用户复测依旧停在 `proposed`。
- 最终方案（确定性、不再依赖任何调度时机）：
  1. **把解析整体移出 setState updater**：新增 `parseAiAgentTurn(conversation, activeRequest)`，在 `finishAiStream` 里**先**用 `aiWorkspaceRef.current` 的会话快照同步解析，产出 `{ content, proposals, terminalActions, mcpActions, shouldRepairAgentProtocol, autoRun }`，再 `setAiWorkspace` 用结果做纯提交；协议纠偏与自动执行都用同步拿到的结果，彻底摆脱 eagerState 时序。
  2. **幂等派发** `dispatchAiAutoRun(conversationId, messageId, action)`（`aiAutoRunDispatchedRef` 去重），同步路径与兜底路径共用，保证不会重复执行同一条命令。
  3. **状态提交兜底**：`aiAutoRunEligibleRef`（在 `beginAiGeneration` 里登记本回合助手消息 id）+ `useEffect([aiWorkspace,...])` 在提交后复查一次；关闭开关时清空白名单，避免「先提案后开开关」把旧命令突然跑掉。
  4. 徽标与运行时选择逻辑对齐（只标注真正会被执行的那一条）；`dispatchAiAutoRun` 打 `[ai-auto-run]` 控制台日志便于排查。
- 排查提示：若用户仍反馈无效，优先怀疑**前端没热更新到位**（App.tsx 的 HMR 可能失败/页面未刷新），让其 F5 或重启 `npm run tauri dev` 后再看控制台是否有 `[ai-auto-run]`。

## 打包 v0.1.2 + 更新 GitHub（用户要求）
- **release 打包成功**：`npm run tauri build`（需先把 `C:\Users\24901\.rustup\toolchains\stable-x86_64-pc-windows-msvc\bin` 加进 `$env:Path`，否则报 `failed to run 'cargo metadata' … program not found`）。产物：
  - `F:\cargo-targets\PandaTerm\release\pandaterm.exe`（30.9 MB）
  - `F:\cargo-targets\PandaTerm\release\bundle\nsis\PandaTerm_0.1.2_x64-setup.exe`（9.7 MB）
  - `F:\cargo-targets\PandaTerm\release\bundle\msi\PandaTerm_0.1.2_x64_en-US.msi`（12.8 MB）
  - 项目 `.cargo/config.toml` 的 target-dir 生效（F:/cargo-targets/PandaTerm）；本次为**首次 release 全量编译**（约 8~10 分钟）。`cargo run`（tauri dev）在跑**不会**阻塞 release 编译、也不会抢文件锁。
- **GitHub 更新（用户选择：合并进 main，非强推）**：
  - 远程有两个：`origin` = codeup.aliyun.com（当前分支跟踪它）；`github` = github.com/Aerial2/PandaTerm。
  - GitHub 的 main 原本是 3 个“Add files via upload”提交、与本地历史**无共同祖先**且落后 76 个提交。
  - 做法：`git worktree add .merge-tmp -b github-main-merge github/main` → `git merge --allow-unrelated-histories -X theirs feat/session-management-reconnect` → `git push github github-main-merge:main`（**fast-forward，非强推**，`ca94f25..2199983`）。用独立 worktree 是为了不打断正在跑的 cargo 编译（切分支会改动 main worktree 的源码）。
  - 本地保留分支 `github-main-merge`（= GitHub main 现状，方便再次推送）；临时 worktree 已删除。
  - 网络提示：期间 github.com:443 多次连接失败（`Could not connect to server`），重试即成功；无代理配置。
- **GitHub Release 未发布**：`gh` CLI 已安装（`C:\Program Files\GitHub CLI\gh.exe`）但**未登录**，需用户先 `gh auth login`，之后再 `gh release create v0.1.2 --repo Aerial2/PandaTerm <两个安装包>`。

## README 加主界面截图并推送（用户要求）
- 用户在对话里发的截图落在 `C:\Users\24901\AppData\Local\Temp\image.png`（IDE 附件的落盘位置），复制进仓库 `docs/images/pandaterm-main-window.png`（122.9 KB），README 第 7 行插入 `![PandaTerm 主界面：…](docs/images/pandaterm-main-window.png)`。
- 提交 `c36ae07 docs: README 增加主界面截图`；推送：`origin`（aliyun）✓、`github` feat 分支 ✓。
- **main 的更新要走合并**：github main（2199983）与 feat 分支不是线性关系（unrelated-history 合并的产物），直接推 feat→main 会被拒。复用的流程：`git worktree add .merge-tmp github-main-merge` → `git merge feat/session-management-reconnect` → `git push github github-main-merge:main` → `git worktree remove .merge-tmp --force`（**偶发 `Permission denied`，重试一次即可删除**）。本次结果：`2199983..79ff720  github-main-merge -> main`，合并后树与 feat 完全一致。
- 注意：截图含真实服务器公网 IP（223.106.47.4）与主机名，已提醒用户。
- 网络：github.com:443 多次瞬断（`Could not connect` / `Recv failure`），重试即可；`gh` 仍未登录，Release 待办。

## GitHub Release v0.1.2 已发布
- 用户完成 `gh auth login`（账号 Aerial2，repo scope）。
- 发布命令：`gh release create v0.1.2 --repo Aerial2/PandaTerm --title "PandaTerm v0.1.2" --notes-file release-notes.md <nsis exe> <msi>`；结果 **Latest**，tag `v0.1.2`，两个附件均已上传：
  - `PandaTerm_0.1.2_x64-setup.exe`（9.7 MB）、`PandaTerm_0.1.2_x64_en-US.msi`（12.8 MB）
  - https://github.com/Aerial2/PandaTerm/releases/tag/v0.1.2
- 仓库为 **PUBLIC**；应用的版本检查（`src-tauri/src/about.rs`）只读 `releases/latest` 的 `tag_name`（失败回退 tags / atom feed），**不依赖附件名**，所以传 exe+msi 都行。
- Release 说明按 v0.1.1 的中文格式写（本版本亮点 / 安装 / 版本检查 / 说明），`release-notes.md` 是临时文件，已删除。
- 用户澄清：截图里的 `223.106.47.4` 是当前网络出口 IP（非固定服务器 IP），无需处理。

## 右键菜单新增「压缩为 TAR.GZ」（用户要求：服务器没装 zip 命令）
- 后端 `create_archive` 增加 `format: Option<String>` 参数（`normalize_archive_format`：`zip`(默认) / `tar.gz`(别名 tgz)，其它报错）。
  - 远程：`tar.gz` 用 `rm -f aq && tar -czf aq -C <parent> <name>`（`-C` 保证归档条目是相对名，与 zip 分支语义对齐）；`zip` 分支保持原样。
  - 本地：`tar.gz` 直接 `Command::new("tar").args(["-czf", archive, "-C", parent, name])`（Windows 10+ 自带 bsdtar），不走 shell 因此无转义问题；`zip` 分支（PowerShell Compress-Archive / sh+zip）不变。
  - 已实测 `tar -czf demo.tar.gz -C . demo` 产出的条目为 `demo/ … demo/sub/a.txt`（扁平、无完整路径）。
- 前端：`api.createArchive(sourcePath, terminalId, format)`；`handleCreateArchive(file, format='zip')`；右键菜单在「压缩为 ZIP」后新增「压缩为 TAR.GZ」。
- 顺带修复：`resourceModel.ts` 的 `ARCHIVE_EXTS` 补上复合后缀 `tar.gz`/`tar.bz2`/`tar.xz`（`getFileExt` 对复合后缀返回带点的整串，原来不在集合里 → `.tar.gz` 文件不显示「解压到当前目录」）。
- 验证：`cargo test` 53 passed（新增 `normalize_archive_format_accepts_zip_and_tar_gz`）、`npx tsc --noEmit` 通过。**改动尚未提交**（用户没要求）。

## 「压缩为」改成二级子菜单（用户嫌格式一多主菜单太长）
- `resourceModel.ts` 新增 `ARCHIVE_FORMAT_OPTIONS`（`{ value: ArchiveFormat; label; hint }[]`），`api.ts` 导出 `ArchiveFormat = 'zip' | 'tar.gz'`。
  - **以后加格式只需两步**：① 在 `ARCHIVE_FORMAT_OPTIONS` 加一项；② 后端 `create_archive` 的 `normalize_archive_format` 支持该值（远程/本地各写一条命令）。
- `App.tsx`：主菜单里原来的「压缩为 ZIP / 压缩为 TAR.GZ」两个按钮替换为一个「压缩为 ▸」宿主 `<div class="file-context-submenu-host">`，悬停（`onMouseEnter/Leave`）或点击展开飞出子菜单。
  - 关键坑：菜单项的点击要 `event.stopPropagation()`，否则 window 上的「点击外部关闭」会先把整个右键菜单收起；子菜单是宿主 DOM 的子节点，所以鼠标移入飞出不触发 `onMouseLeave`。
  - 贴右边缘时自动 `flip-left` 向左展开（启发式：`contextMenu.x + 480 > window.innerWidth`）。
  - 新增状态 `archiveSubmenuOpen`，并用 `useEffect([contextMenu])` 在每次打开/关闭右键菜单时收起子菜单。
- `styles.css`：新增 `.file-context-submenu-host / -arrow / .file-context-submenu(.flip-left)` 与子项右侧 `em` 提示样式。
- 验证：`npx tsc --noEmit` 通过。（改动仍未提交）

## 远程缺 zip 时自动降级为 tar.gz（用户实测报错 `bash: zip: command not found`）
- `create_archive` 两条路径都加了兜底：zip 命令不存在时 shell 返回 **退出码 127**，此时自动改用 `tar.gz` 重跑一次，并返回 `.tar.gz` 路径（用户无需自己选格式）。
  - 远程分支：把「生成某格式的命令 + 目标路径」抽成 `build_remote(format)` 闭包，主命令失败且 `format == zip && exit_code == 127` 时用 `build_remote("tar.gz")` 重试。
  - 本地分支：仅 Unix（`!cfg!(windows)`）在 `output.status.code() == Some(127)` 时用 `tar` 重试（Windows 走 PowerShell Compress-Archive，不存在缺命令问题）。
  - 新增 `command_failure_detail(stderr, exit_code)` 统一错误信息；两条命令都失败时错误里写明「未安装 zip，改用 tar.gz 也失败」。
- 前端 `handleCreateArchive`：请求 zip 但返回路径以 `.tar.gz` 结尾时，状态栏/日志提示「未安装 zip 命令，已自动改用 tar.gz」。
- ⚠️ 必须重启 `npm run tauri dev`：这是 Rust 改动，且旧后端会**忽略**未知的 `format` 参数（serde 默认忽略多余字段），不重启的话选 TAR.GZ 也会静默走 zip 并报同样的错。
- 验证：`cargo test` 53 passed、`npx tsc --noEmit` 通过。

## 缺命令改为「弹窗确认换格式」（用户要求：不要偷偷换，要先提醒确认）
- 后端 `create_archive` 返回值由 `String` 改为结构体 `CreateArchiveResult{ path: Option<String>, format, missing_tool: Option<String>, suggested_format: Option<String> }`：
  - 成功 → `CreateArchiveResult::done(path, format)`；
  - 缺命令（远程 shell 退出码 **127**，或本地 `Command::new` 报 `ErrorKind::NotFound`）→ `CreateArchiveResult::needs_tool(format)`，**不再自行重试**，`path = None`；
  - 其它失败仍返回 `Err`。
  - 辅助：`archive_format_tool(format)`（zip→"zip"，tar.gz→"tar"）、`archive_alternative_format()`。
- 前端 `api.createArchive` 返回 `CreateArchiveResult`；`handleCreateArchive(file, format, allowFormatSwitch = true)` 在 `path` 为空且有 `missing_tool` 时弹 `setConfirmDialog`：
  - 标题「zip 命令不可用」，正文「目标主机上没有找到 zip 命令，无法压缩为 ZIP。是否改用 TAR.GZ 重新压缩？」，确认按钮「改用 TAR.GZ」；
  - 确认后以 `allowFormatSwitch=false` 递归调用，避免格式来回弹窗；再次缺命令则直接报错。
  - `resourceModel.archiveFormatLabel(format)` 统一取展示名（来自 `ARCHIVE_FORMAT_OPTIONS`）。
- 验证：`cargo test` 53 passed、`npx tsc --noEmit` 通过。仍需重启 `npm run tauri dev` 才生效（Rust 改动 + 返回值结构变了）。

## 前一日（09-09）遗留说明
- 前一天完成的大量改动（分屏/编辑器归属、保存链路、Rust 优化、MCP 竞态、RDP 剪贴板等）详见 `2026-09-09.md`；Rust 部分需重启 `npm run tauri dev` 生效，建议验证后提交 commit。
